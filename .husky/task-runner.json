{
   "tasks": [
        {
            "name": "dotnet-format",
            "command": "dotnet",
            "group": "pre-commit",
            "args": ["format", "--include" , "${staged}"],
            "include": ["**/*.cs"]
        }
    ]
}
