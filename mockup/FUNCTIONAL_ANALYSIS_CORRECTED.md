# PwSafe Client 功能分析 (基于官方Password Safe规范)

## 重要说明 (Important Notice)

**本文档是PwSafe客户端的功能分析，严格基于Password Safe官方规范。**

This document analyzes the PwSafeClient mockup functionality **based strictly on the official Password Safe specification**, not generic password manager features.

---

## Password Safe 官方规范参考

- **官方网站:** https://www.pwsafe.org/
- **格式规范:** 
  - [Password Safe v3 Format](https://github.com/pwsafe/pwsafe/blob/master/docs/formatV3.txt)
  - [Password Safe v4 Format](https://github.com/pwsafe/pwsafe/blob/master/docs/formatV4.txt)
- **官方实现:** https://github.com/pwsafe/pwsafe

---

## 目录 (Table of Contents)

1. [Password Safe 支持的功能](#1-password-safe-支持的功能)
2. [Mockup中缺少的官方功能](#2-mockup中缺少的官方功能)
3. [Mockup中的不合理流程](#3-mockup中的不合理流程)
4. [实施优先级](#4-实施优先级)

---

## 1. Password Safe 支持的功能

### 1.1 Password Safe 的条目类型

根据官方规范，Password Safe **只有以下类型**：

#### ✅ 标准条目 (Standard Entry)
每个条目包含以下字段：
- **Title** (必需) - 0x01
- **Username** - 0x02
- **Password** (必需) - 0x04
- **Notes** - 0x03
- **URL** - 0x0B
- **Email** - 0x14
- **Group** (用于文件夹组织)
- **UUID** - 0x0A (条目唯一标识)
- **Creation Time** - 0x05
- **Password Modification Time** - 0x06
- **Last Access Time** - 0x07
- **Password Expiry Time** - 0x08
- **Password Policy** - 0x09
- **Password History** - 0x0F (历史密码记录)
- **Autotype** - 0x0C
- **Run Command** - 0x12
- **Protected Entry** - 0x13

#### ✅ Alias条目 (Alias Entry)
- 引用另一个条目的密码
- 用于共享密码的场景
- 语法：`[base]` 或 `[group.title.username]`

#### ✅ 快捷方式条目 (Shortcut Entry)
- 引用另一个完整条目（包括用户名和密码）
- 与Alias不同，Shortcut引用整个条目

#### ✅ 附件 (Attachments)
- 从v3.70开始支持
- 每个条目可以有**一个**文件附件
- v4格式中的字段：0x22 (内容), 0x23 (文件名), 0x24 (类型)

#### ❌ Password Safe **不支持**的类型
- ❌ 信用卡 (Credit Card) - 不是独立类型
- ❌ 身份信息 (Identity) - 不是独立类型
- ❌ 银行账户 (Bank Account) - 不是独立类型
- ❌ 软件许可证 (License) - 不是独立类型
- ❌ SSH密钥 (SSH Key) - 不是独立类型

**重要：** Password Safe使用**统一的Entry结构**，所有敏感信息都存储为标准条目，通过Notes字段记录额外信息。

---

### 1.2 Password Safe 支持的核心功能

#### ✅ 密码历史 (Password History)
**官方文档确认：Password Safe原生支持密码历史功能**

- 字段类型：0x0F
- 格式：`nn nn YYYYMMDDHHMMSS password`
- 存储每个密码的时间戳和内容
- 可配置保存数量（全局或per-entry）
- 在Entry编辑对话框的"Additional"标签页中显示

**Mockup状态：** ❌ 缺少密码历史功能

**影响：**
- 用户无法查看历史密码
- 无法回退到之前的密码
- 不符合Password Safe官方规范

**解决方案：**
```
添加密码历史功能：
1. Entry Detail页面添加"Password History"部分
2. 显示格式：
   - 日期时间：2024-01-15 14:30:25
   - 密码：[Show] [Copy] 按钮
3. 点击历史密码可以：
   - 显示密码
   - 复制到剪贴板
   - 恢复为当前密码（需确认）
4. 在Settings中配置：
   - 保存的历史密码数量（默认5个）
   - 是否对所有条目启用
```

---

#### ✅ 组/文件夹 (Groups/Folders)
- Password Safe使用**点分隔**的层级结构
- 例如：`Banking.Credit Cards.Chase`
- 组名存储在Entry的Group字段中
- 不是独立的记录类型

**Mockup状态：** ✅ 已支持基本文件夹功能

**改进建议：**
- 支持多级嵌套（如 Group.SubGroup.SubSubGroup）
- 显示完整路径面包屑
- 支持拖拽移动条目到不同组

---

#### ✅ 密码策略 (Password Policy)
- 字段类型：0x09
- 可以per-entry设置
- 也可以使用命名策略（Named Policy）
- 包括：长度、字符集、排除字符等

**Mockup状态：** ✅ Settings中有提及，但不够详细

**改进建议：**
```
完善密码策略功能：
1. 在Generate Password时应用策略
2. Settings中管理命名策略：
   - 策略名称（如"High Security", "Legacy Systems"）
   - 长度范围
   - 字符集（大小写、数字、符号）
   - 排除的字符
   - 密码模式
3. 在Add/Edit Entry时选择策略
4. 验证密码是否符合策略
```

---

#### ✅ 密码过期 (Password Expiry)
- 字段类型：0x08
- 存储密码的有效期
- 可以设置过期日期或天数

**Mockup状态：** ❌ 没有过期提醒功能

**解决方案：**
```
添加密码过期功能：
1. 在Entry Detail显示过期状态
2. 过期前7天开始警告
3. 过期后标记为红色
4. 在Password List中显示过期图标
5. Settings中配置默认有效期（如90天）
```

---

#### ✅ 双因子认证字段 (Two-Factor/OTP)
- v3格式从3.40版本开始支持
- 字段类型：0x16 (Two-Factor Key)
- 存储TOTP密钥，但**不生成OTP**
- 官方pwsafe不内置TOTP生成器

**说明：** Password Safe本身**不是TOTP生成器**，只存储密钥。用户需要使用其他工具生成验证码。

**Mockup建议：**
- 可以添加"Two-Factor Key"字段存储
- 但不应该实现TOTP生成器（超出pwsafe范围）
- 或者作为可选的增强功能（明确标注为扩展）

---

#### ✅ Autotype功能
- 字段类型：0x0C
- 定义自动输入序列
- 格式：`\u\t\p\n` (username, tab, password, enter)

**Mockup状态：** ❌ 未提及

**建议：** 移动端可以不实现（桌面端功能）

---

### 1.3 Password Safe **不支持**的功能

#### ❌ 回收站 (Recycle Bin)
**官方确认：Password Safe没有回收站功能**
- 删除是永久性的
- 依赖备份恢复误删除的条目

**Mockup建议：**
- 删除前必须有明确的确认对话框
- 建议："This will permanently delete the entry. This cannot be undone."
- 提供定期备份功能

---

#### ❌ 标签系统 (Tags/Labels)
**官方确认：Password Safe只有Groups，没有Tags**
- 只能通过Group字段组织
- 不支持多标签分类
- 一个条目只能属于一个Group

**Mockup建议：**
- 不要添加标签功能（与格式不兼容）
- 使用Group的层级结构组织条目
- 可以通过搜索弥补标签的缺失

---

#### ❌ 自定义字段 (Custom Fields)
**官方确认：Password Safe v3/v4格式是固定schema**
- 字段类型是预定义的（0x01-0x24等）
- 不支持用户自定义字段类型
- 额外信息只能存储在Notes字段中

**Mockup建议：**
- 不要承诺自定义字段功能
- 充分利用Notes字段
- 可以提供Notes模板（如"Email: xxx\nPhone: xxx"）

---

#### ❌ 多种条目类型 (Multiple Entry Types)
**官方确认：所有条目使用相同的结构**
- 没有"信用卡"、"身份信息"等独立类型
- 所有敏感信息都是"Entry"
- 通过Notes字段记录不同类型的信息

**Mockup建议：**
- 不要设计多种Entry类型的UI
- 可以提供Notes模板建议：
  ```
  Credit Card Template:
  Card Number: 
  CVV: 
  Expiry: 
  Holder Name: 
  
  Identity Template:
  ID Number:
  Issue Date:
  Expiry Date:
  ```

---

## 2. Mockup中缺少的官方功能

### 2.1 密码历史查看
**严重程度：** 🔴 高

**现状：** Entry Detail页面只显示当前密码

**pwsafe支持：** ✅ 是，0x0F字段

**解决方案：**
```
在Entry Detail页面添加"Password History"部分：

┌─────────────────────────────────┐
│ Password History                │
├─────────────────────────────────┤
│ 2024-01-20 15:30                │
│ ••••••••••••  [Show] [Copy]     │
├─────────────────────────────────┤
│ 2023-10-15 09:45                │
│ ••••••••••••  [Show] [Copy]     │
├─────────────────────────────────┤
│ 2023-07-03 14:20                │
│ ••••••••••••  [Show] [Copy]     │
└─────────────────────────────────┘

Settings:
☑ Save password history
  Maximum entries to keep: [5] ▼
```

---

### 2.2 密码过期提醒
**严重程度：** 🟡 中

**现状：** 没有过期相关功能

**pwsafe支持：** ✅ 是，0x08字段

**解决方案：**
```
1. Entry Detail显示：
   ⚠️ Password expires in 7 days
   
2. Password List中：
   Netflix [!] (过期图标)
   
3. Settings:
   Default password lifetime: [90] days
   Warn before expiry: [7] days
```

---

### 2.3 Alias和Shortcut支持
**严重程度：** 🟡 中

**现状：** 没有看到Alias条目支持

**pwsafe支持：** ✅ 是，通过Password字段引用

**解决方案：**
```
添加Alias Entry类型：

Create New → 
  ○ Password Entry
  ○ Alias Entry (uses password from another entry)
  ○ Shortcut Entry (links to another entry)

Alias配置：
  Base Entry: [Select Entry...] ▼
  Display: [g:Netflix]
  
在列表中显示：
  Netflix Login → [Alias icon]
```

---

### 2.4 附件管理
**严重程度：** 🟢 低（v3.70+才支持）

**现状：** 没有附件功能

**pwsafe支持：** ✅ 是（v3.70+），每个条目一个文件

**解决方案：**
```
在Entry Detail添加"Attachment"部分：
┌─────────────────────────────────┐
│ Attachment                      │
├─────────────────────────────────┤
│ 📎 recovery_codes.txt (2.3 KB)  │
│ [View] [Download] [Remove]      │
│                                 │
│ [+ Add Attachment]              │
│ (Only one file allowed)         │
└─────────────────────────────────┘
```

---

### 2.5 密码策略配置
**严重程度：** 🟡 中

**现状：** Settings中有提及但不详细

**pwsafe支持：** ✅ 是，0x09字段

**解决方案：**
```
Settings → Password Policies:

Named Policies:
┌─────────────────────────────────┐
│ • Default (16 chars, all types) │
│ • High Security (20+, complex)  │
│ • Legacy (8-12, alphanumeric)   │
│ [+ Add Policy]                  │
└─────────────────────────────────┘

Policy Configuration:
  Name: [High Security]
  Length: Min [20] Max [32]
  ☑ Lowercase (a-z)
  ☑ Uppercase (A-Z)
  ☑ Digits (0-9)
  ☑ Symbols (!@#$...)
  ☐ Exclude ambiguous (0O1lI)
  
在Generate Password时：
  Use policy: [High Security] ▼
```

---

## 3. Mockup中的不合理流程

### 3.1 删除条目无警告
**问题：** 删除是永久性的（pwsafe无回收站），但Delete按钮没有明确警告

**解决方案：**
```
点击Delete后弹出确认对话框：

┌─────────────────────────────────┐
│ ⚠️  Delete Entry                │
├─────────────────────────────────┤
│ Are you sure you want to        │
│ permanently delete "Netflix"?   │
│                                 │
│ This action CANNOT be undone.   │
│ Consider backing up your        │
│ database first.                 │
├─────────────────────────────────┤
│ [Cancel]  [Delete Permanently]  │
└─────────────────────────────────┘

Delete按钮应该是红色警告样式
```

---

### 3.2 导入导出格式
**问题：** 没有明确说明支持的格式

**pwsafe官方支持：**
- ✅ .psafe3 (v3格式，推荐)
- ✅ .dat (v1/v2格式，旧版)
- ✅ .txt (明文导出，不推荐)
- ✅ .xml (明文导出)

**解决方案：**
```
Import:
  Supported formats:
  • Password Safe (.psafe3, .dat)
  • Plain Text (.txt, .csv) - ⚠️ unencrypted
  • XML (.xml) - ⚠️ unencrypted
  
Export:
  ○ Password Safe format (.psafe3) - Recommended
  ○ Plain Text (.txt) - ⚠️ NOT secure
  ○ XML (.xml) - ⚠️ NOT secure
  
  [ ] Include password history
  [ ] Include attachments
```

---

### 3.3 同步功能过度承诺
**问题：** 提到"Cloud Sync"，但pwsafe本身不提供云同步服务

**pwsafe实际情况：**
- 官方pwsafe **不提供云同步服务**
- 用户需要手动将.psafe3文件放在云盘（Dropbox, OneDrive等）
- 依赖文件系统的同步，pwsafe只负责处理文件锁定

**解决方案：**
```
Settings → Sync:

Database Location:
○ Local only
○ Cloud storage folder
  (Store in Dropbox/OneDrive/etc.)
  
⚠️ Note: Password Safe does not provide
cloud sync service. You need to place
the .psafe3 file in a cloud-synced folder.

Current location:
/Users/xxx/Dropbox/Personal.psafe3

[Change Location...]

Sync conflicts:
When file is modified elsewhere:
○ Always ask which version to keep
○ Auto-use newest version
○ Auto-use local version
```

---

## 4. 实施优先级

### Phase 1: 修复与官方规范的差异 (2-3周)

#### P0 - 关键功能（必须实现）

1. **密码历史功能** (3天)
   - 显示历史密码列表
   - 复制历史密码
   - 恢复历史密码
   - 配置保存数量

2. **删除确认对话框** (1天)
   - 明确警告永久删除
   - 建议备份
   - 二次确认

3. **明确同步机制** (2天)
   - 移除"Cloud Sync"误导
   - 说明文件位置选择
   - 处理文件冲突

4. **密码过期提醒** (3天)
   - 显示过期状态
   - 过期警告
   - 配置默认有效期

**小计：9天**

---

### Phase 2: 增强Password Safe特有功能 (2-3周)

#### P1 - 高优先级

1. **Alias/Shortcut条目** (4天)
   - 创建Alias条目
   - 引用base entry
   - 列表中显示类型

2. **密码策略管理** (3天)
   - 命名策略配置
   - 策略应用到生成器
   - per-entry策略设置

3. **附件支持** (4天)
   - 上传附件（单文件）
   - 下载附件
   - 删除附件
   - 显示附件信息

4. **改进Group管理** (3天)
   - 多级嵌套支持
   - 完整路径面包屑
   - 拖拽移动

**小计：14天**

---

### Phase 3: 导入导出和兼容性 (1-2周)

#### P2 - 中优先级

1. **完善导入功能** (3天)
   - 支持.psafe3格式
   - 支持v1/v2 .dat格式
   - 明文格式警告

2. **完善导出功能** (3天)
   - 选择导出格式
   - 包含/排除选项
   - 安全警告

3. **改进搜索** (2天)
   - 搜索所有字段（包括Notes）
   - 高亮匹配结果
   - 搜索历史

**小计：8天**

---

### 不实施的功能（与pwsafe不兼容）

#### ❌ 明确不做的功能

1. ❌ **回收站** - pwsafe格式不支持
2. ❌ **标签系统** - 只有Groups
3. ❌ **自定义字段** - 固定schema
4. ❌ **多种条目类型** - 统一Entry结构
5. ❌ **内置TOTP生成器** - 超出pwsafe范围
6. ❌ **云同步服务** - pwsafe不提供
7. ❌ **共享功能** - 单用户设计
8. ❌ **版本控制** - 依赖备份
9. ❌ **审计日志** - 格式不支持
10. ❌ **批量操作** - 可以实现但非优先

这些功能如果要实现，需要扩展pwsafe格式或使用额外的元数据文件，这会破坏与官方pwsafe的兼容性。

---

## 5. 总结

### 修正后的统计

**需要实现的功能：** 11个（而非之前的42个）
**预估总工时：** 31天（约6周，而非76-104天）

### 优先级分布

| 优先级 | 功能数 | 工时 | 说明 |
|--------|--------|------|------|
| P0 | 4 | 9天 | 修复与规范的差异 |
| P1 | 4 | 14天 | 增强官方功能 |
| P2 | 3 | 8天 | 导入导出兼容性 |
| **总计** | **11** | **31天** | **~6周** |

### 核心原则

1. **严格遵守pwsafe格式规范**
   - 不添加格式不支持的功能
   - 保持与官方pwsafe的兼容性

2. **不要过度承诺**
   - 明确说明这是pwsafe客户端
   - 不误导用户期望通用密码管理器功能

3. **专注核心价值**
   - 做好pwsafe格式的读写
   - 提供良好的移动端体验
   - 完整支持官方定义的所有字段

4. **清晰的功能边界**
   - 在UI中明确标注pwsafe限制
   - 提供解释和替代方案
   - 避免用户混淆

---

## 参考资源

### 官方文档
- [Password Safe 官方网站](https://www.pwsafe.org/)
- [Password Safe v3 格式规范](https://github.com/pwsafe/pwsafe/blob/master/docs/formatV3.txt)
- [Password Safe v4 格式规范](https://github.com/pwsafe/pwsafe/blob/master/docs/formatV4.txt)
- [Password Safe 官方仓库](https://github.com/pwsafe/pwsafe)

### 实现参考
- [Medo.PasswordSafe](https://github.com/medo64/Medo.PasswordSafe) - C# library
- [pypwsafe](https://github.com/ronys/pypwsafe) - Python library

---

**文档版本：** 1.1 (已修正)  
**最后更新：** 2024-01-26  
**状态：** 基于官方pwsafe规范修正完成
